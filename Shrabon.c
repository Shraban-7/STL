#include<stdio.h>
#define MAX 100
{


    void traverse(int array[],int size)
    void search(int array[],int size)
    //void sort(int array[],int size)
    //void deletion(int array[],int size)
   // void insertion(int array[],int size)
    //int marging(int array[],int size)
}
int main()
{
    int size =10, option,i;
    int def_array[MAX] = {1,3,5,6,4,7,11,9,15,16};
    printf("Do you create your own array ?\n");
    printf("\n");
    printf("1.YES\n2.NO\n");
    scanf("%d",&option);

    if(option=1)
    {
        printf("Enter size of element:");
    }
   /* printf("Which position ? ");
    scanf("%d",&pos);
    printf("Enter the value =");
    scanf("%d",&value);
    for(i=size; i>=pos; i--) //arr insert;
    {
        arr[i]=arr[i-1];
    }
    arr[pos-1]=value;
    for(i=0; i<size+1; i++)
    {
        printf("%d ",arr[i]);
    }
    printf("\n");
    //arr delete
    printf("Position =");
    scanf("%d",&pos);
    for(i=pos-1; i<=size-2; i++)
    {
        arr[i]=arr[i+1];
    }
    arr[size-1]=0;
    for(i=0; i<size; i++)
    {
        printf("%d ",arr[i]);
    }*/
    return 0;
}
