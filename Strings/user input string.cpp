#include<iostream>
#include<cstdio>
#include<string>

using namespace std;
int main()
{
    //gets & puts function use only use array of character ;
   /* char a[20];
    gets(a);
    puts(a);
    return 0;*/
    string a;
    getline(cin,a);
    cout<<a<<endl;
}
